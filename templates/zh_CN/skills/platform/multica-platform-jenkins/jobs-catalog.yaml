# cicd 空间 Job 清单（dev / sit）
# 逻辑 service 名 → Jenkins Job 全名；dev/sit 名称不一致处需显式标注
# 说明：原文件包含公司具体微服务清单，已脱敏为占位骨架。
#       使用方请按自己团队的「service 名 → Jenkins job 全名」映射填写。
version: "1.0"

services:
  # 示例（请替换为你的服务）：
  # <service-a>:
  #   dev: <jenkins-job-dev-a>
  #   sit: <jenkins-job-sit-a>
  # <service-b>:
  #   dev: <jenkins-job-dev-b>
  # <service-c>:
  #   sit: <jenkins-job-sit-c>
